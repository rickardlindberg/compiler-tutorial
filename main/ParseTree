main.ParseTree

A parse tree is a tree representing the structure of a program.

Program:

    let main =
        \() -> cScale
        exit

Parse tree:

![Parse tree.]($ROOT/static/images/parse-tree.png)
