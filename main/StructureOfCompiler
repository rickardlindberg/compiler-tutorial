main.StructureOfCompiler

A compiler is usually written by doing a sequence of transformations to the
source program until the target program is obtained.

![Stages in a copmiler.]($ROOT/static/images/stages.png)

The reason is to handle the complexity. Transforming source text into target
text is not always straight forward.

    show how going direct is hard

Different compilers have different stages. Follow along to see what stages are
involved in the compiler for this tutorial.

## Continue

ConOverview
